node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Local JSON store (used only when DATABASE_URL is unset)
.data

# Secrets
.env*
!.env.example

.vercel

# Generated by `npm run build:api`. Not ignored on purpose: Vercel scans the
# api/ directory to discover serverless functions, so the file must be present
# in the upload — the build step then regenerates it.
# api/
